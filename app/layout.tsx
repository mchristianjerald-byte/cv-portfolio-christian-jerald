import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Christian Jerald Martinez | AI Agent Developer",
  description:
    "Portfolio of Christian Jerald U. Martinez - BSIT student and AI Agent Developer intern specializing in Full-Stack development, Next.js, and MCP Server Architecture.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} antialiased bg-white text-gray-900`}>
        {children}
      </body>
    </html>
  );
}
